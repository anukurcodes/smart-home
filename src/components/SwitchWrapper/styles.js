import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  switchBoxContainer: {
    display: 'flex',
    gap: 15,
    justifyContent: 'space-around',
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
});
